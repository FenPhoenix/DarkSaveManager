namespace DarkSaveManager;

// TODO: Temp just to have all the AL-pasted code work for now

internal static class LText
{
    internal static class AlertMessages
    {
        internal static readonly string Error = "Error";
        internal static readonly string Error_ViewLog = "View log";
    }

    internal static class Global
    {
        internal static readonly string OK = "OK";
        internal static readonly string SelectAll = "Select all";
    }
}
